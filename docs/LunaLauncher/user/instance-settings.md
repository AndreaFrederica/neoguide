# 实例设置

> **⚠️ 免责声明**：本文档由 **MiniMax-M2.1** 自动生成，内容可能存在错误或不完整之处。**请以人工书写的官方为准**。

---

实例设置允许你微调每个 Minecraft 实例的运行参数。

## Java 设置

### Java 版本

选择合适的 Java 版本：
- Minecraft 1.17+ 需要 Java 17+
- 旧版本使用 Java 8 或 11

### JVM 参数

自定义 JVM 启动参数：

```bash
-Xmx4G          # 最大内存
-Xms1G          # 最小内存
-XX:+UseG1GC    # 使用 G1 垃圾收集器
```

## 游戏设置

### 游戏目录

- **默认**：实例目录下的 .minecraft
- **自定义**：指定其他位置

### 游戏窗口

- 分辨率设置
- 全屏模式
- 垂直同步

### 性能优化

- 图形渲染距离
- 模拟距离
- 帧率上限

## 账户设置

### 选择账户

为实例选择游戏账户：
- 使用默认账户
- 指定特定账户

### 离线模式

允许使用自定义玩家名称（需要关闭正版验证）。

## 高级设置

### 环境变量

设置自定义环境变量。

### 启动脚本

自定义游戏启动前的脚本命令。

---

根据你的硬件配置调整这些设置，可以获得最佳游戏体验。
